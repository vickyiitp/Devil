// Runtime configuration - override API host without rebuilding the app
// Replace the value during deployment with your backend URL.
// Example:
// window.__API_URL = 'https://api.vickyiitp.tech';
window.__API_URL = '';
